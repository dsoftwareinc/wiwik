apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wiwik.fullname" . }}-test-connection"
  labels:
    {{- include "wiwik.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wiwik.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
