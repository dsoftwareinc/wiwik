apiVersion: v1
kind: Service
metadata:
  name: {{ include "wiwik.fullname" . }}
  labels:
    {{- include "wiwik.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wiwik.selectorLabels" . | nindent 4 }}
